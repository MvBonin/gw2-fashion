# Site URL for absolute links, auth redirects, OG, sitemap. Set to https://gw2-fashion.com in production (required for login callback).
NEXT_PUBLIC_SITE_URL=https://gw2-fashion.com

NEXT_PUBLIC_SUPABASE_URL=http://localhost:3000

NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_URL=###.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Umami analytics (optional – leave empty to disable)
NEXT_PUBLIC_UMAMI_SCRIPT_URL=
NEXT_PUBLIC_UMAMI_WEBSITE_ID=

# Cron: secret for cron endpoints (e.g. update-trending-buckets). Set in production.
CRON_SECRET=

# Optional: Service Role Key for copy-count fallback when RPC fails (server-only, do not expose).
# SUPABASE_SERVICE_ROLE_KEY=

